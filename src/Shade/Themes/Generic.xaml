<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=System.Runtime">

    <!-- ── Brand palette (mirrors Shade's SwiftUI tokens) ──────────────── -->
    <Color x:Key="BgTopColor">#FF12141C</Color>
    <Color x:Key="BgBottomColor">#FF1A1C29</Color>
    <Color x:Key="GlassFillColor">#14FFFFFF</Color>
    <Color x:Key="GlassStrokeColor">#1FFFFFFF</Color>
    <Color x:Key="DarkInputColor">#40000000</Color>

    <SolidColorBrush x:Key="GlassFill" Color="{StaticResource GlassFillColor}" />
    <SolidColorBrush x:Key="GlassStroke" Color="{StaticResource GlassStrokeColor}" />
    <SolidColorBrush x:Key="DarkInput" Color="{StaticResource DarkInputColor}" />

    <SolidColorBrush x:Key="Primary" Color="#FFEFEFEF" />
    <SolidColorBrush x:Key="Secondary" Color="#B8B8B8" />
    <SolidColorBrush x:Key="Tertiary" Color="#80B0B0B0" />

    <SolidColorBrush x:Key="AccentPurple" Color="#FFB191FF" />
    <SolidColorBrush x:Key="AccentPurpleDim" Color="#33B191FF" />
    <SolidColorBrush x:Key="AccentGreen" Color="#FF34D399" />
    <SolidColorBrush x:Key="AccentRed" Color="#FFEF4444" />
    <SolidColorBrush x:Key="AccentYellow" Color="#FFEAB308" />
    <SolidColorBrush x:Key="AccentCyan" Color="#FF22D3EE" />
    <SolidColorBrush x:Key="AccentMint" Color="#FF34D399" />
    <SolidColorBrush x:Key="AccentOrange" Color="#FFFB923C" />

    <LinearGradientBrush x:Key="WindowBackground" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource BgTopColor}" Offset="0.0" />
        <GradientStop Color="{StaticResource BgBottomColor}" Offset="1.0" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="StartButtonBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#FF7C5BFF" Offset="0.0" />
        <GradientStop Color="#FF5F3BE5" Offset="1.0" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="StopButtonBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#FFE53E3E" Offset="0.0" />
        <GradientStop Color="#FFC53030" Offset="1.0" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="DownBarBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#7322D3EE" Offset="0.0" />
        <GradientStop Color="#FF22D3EE" Offset="1.0" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="UpBarBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#73B191FF" Offset="0.0" />
        <GradientStop Color="#FFB191FF" Offset="1.0" />
    </LinearGradientBrush>

    <!-- ── Card ────────────────────────────────────────────────────────── -->
    <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource GlassFill}" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassStroke}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="14" />
        <Setter Property="Padding" Value="18" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style x:Key="InputCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource DarkInput}" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassStroke}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="10,8" />
    </Style>

    <!-- ── Default text colors ─────────────────────────────────────────── -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Primary}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Ideal" />
    </Style>

    <Style x:Key="MutedText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Secondary}" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <Style x:Key="SmallText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Secondary}" />
        <Setter Property="FontSize" Value="11" />
    </Style>

    <Style x:Key="MonoText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Consolas, Cascadia Mono, Courier New" />
        <Setter Property="Foreground" Value="{StaticResource Secondary}" />
        <Setter Property="FontSize" Value="11" />
    </Style>

    <Style x:Key="Heading" TargetType="TextBlock">
        <Setter Property="FontSize" Value="22" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource Primary}" />
    </Style>

    <!-- ── TextBox ─────────────────────────────────────────────────────── -->
    <Style TargetType="TextBox">
        <Setter Property="Foreground" Value="{StaticResource Primary}" />
        <Setter Property="Background" Value="{StaticResource DarkInput}" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassStroke}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="FontFamily" Value="Consolas, Cascadia Mono, Segoe UI" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="CaretBrush" Value="{StaticResource AccentPurple}" />
        <Setter Property="SelectionBrush" Value="{StaticResource AccentPurpleDim}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      VerticalAlignment="Center"
                                      Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentPurple}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="PasswordBox" BasedOn="{x:Null}">
        <Setter Property="Foreground" Value="{StaticResource Primary}" />
        <Setter Property="Background" Value="{StaticResource DarkInput}" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassStroke}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="FontFamily" Value="Consolas, Cascadia Mono, Segoe UI" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="CaretBrush" Value="{StaticResource AccentPurple}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="PasswordBox">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentPurple}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── Button styles ───────────────────────────────────────────────── -->
    <Style x:Key="GhostButton" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource Primary}" />
        <Setter Property="Background" Value="{StaticResource GlassFill}" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassStroke}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#26FFFFFF" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#33FFFFFF" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource GhostButton}">
        <Setter Property="Background" Value="{StaticResource AccentPurple}" />
        <Setter Property="Foreground" Value="#FF111122" />
        <Setter Property="BorderBrush" Value="Transparent" />
    </Style>

    <Style x:Key="LinkButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource AccentPurple}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <ContentPresenter />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── ToggleSwitch (a CheckBox styled as a switch) ────────────────── -->
    <Style x:Key="ToggleSwitch" TargetType="CheckBox">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid>
                        <Border x:Name="Track" Width="38" Height="20" CornerRadius="10"
                                Background="#26FFFFFF" />
                        <Border x:Name="Thumb" Width="16" Height="16" CornerRadius="8"
                                Background="#FFEFEFEF"
                                HorizontalAlignment="Left"
                                Margin="2,0,0,0" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Track" Property="Background" Value="{StaticResource AccentPurple}" />
                            <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" />
                            <Setter TargetName="Thumb" Property="Margin" Value="0,0,2,0" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── Sidebar item button ─────────────────────────────────────────── -->
    <Style x:Key="SidebarItem" TargetType="ToggleButton">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="Bd" CornerRadius="8" Padding="10,8" Background="Transparent">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#10FFFFFF" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentPurpleDim}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── ScrollBar — minimal dark ────────────────────────────────────── -->
    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Width" Value="6" />
    </Style>

    <!-- ── ComboBox — minimal dark (used by LB strategy picker) ────────── -->
    <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="{StaticResource Primary}" />
        <Setter Property="Background" Value="{StaticResource DarkInput}" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassStroke}" />
        <Setter Property="Padding" Value="10,4" />
    </Style>
</ResourceDictionary>
